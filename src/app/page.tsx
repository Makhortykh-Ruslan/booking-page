'use client';

import { JSX } from 'react';

import BookingSelection from '@/app/main/components/BookingSelection/BookingSelection';

export default function Page(): JSX.Element {
  return <BookingSelection />;
}
